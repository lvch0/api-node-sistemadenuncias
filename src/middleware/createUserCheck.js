const emptyName(j) => {
    if (!j) {
        throw new Error("Se debe ingresar un nombre para la creación de usuario.")
    }
}

const nameUpperCase(j) => {
    if (/^\s+$/.test(f)) {
        throw new Error("No es posible ingresar espacios en blanco.")
    }
}

